import React from 'react';
import AuthContainer from "@/components/auth/AuthContainer";

const Page = () => {
    return (
        <div>
            <AuthContainer/>
        </div>
    );
};

export default Page;